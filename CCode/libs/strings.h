
/* Returns the length of string "str".  */
int length (char str[]);

/* Reads from stdin into "s" using getchar, up to "lim" characters.*/
int getFile (char s[], int lim);

/* Returns the first position of "srch" in "str".  
   Return -1 if srch not found. */
int substr (char str[], char srch[]);
